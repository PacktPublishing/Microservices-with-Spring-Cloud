spring:
  application.name: 'serviceregistry'
  cloud:
    config:
      failFast: true
      retry:
        max-attempts: 60
      discovery:
        enabled: false
      uri: 'http://localhost:8888'

